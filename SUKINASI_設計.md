## **1\. 主要クラス設計とコンポーネント**

システムを制御する2つの主要スクリプトの変数およびメソッド設計です。

| クラス名 | 主要変数 / プロパティ | 主要メソッド |
| :---- | :---- | :---- |
| **kyori.cs** | startPoint, endPoint (3D座標) distanceText (UI描画用) | SetPoint(): 始点・終点の打点記録 ResetMeasurement(): データの初期化 |
| **RakutenSearch.cs** | tolerance (許容誤差) maxBatches (通信上限) itemCardPrefab (UI生成用) | BuildQueries(): 分割検索文字列の生成 SendRequest(): APIへの非同期通信 InstantiateCard(): 商品パネルの生成 |

## 

## 

## 

## 

## 

## 

## **2\. AR空間座標の寸法算出モデル**

AR FoundationのRaycast機能で取得した空間上の2点 P1(x1,y1,z1) と P2(x2,y2,z2) から、各寸法の絶対値を以下の数式に基づいて算出します。

$$W \= \\vert{}x\_2 \- x\_1\\vert{}$$

$$H \= \\vert{}y\_2 \- y\_1\\vert{}$$

$$D \= \\vert{}z\_2 \- z\_1\\vert{}$$

ここで算出された W（幅）、H（高さ）、D（奥行）がセンチメートル単位へ変換され、検索のベース値となります。

## **3\. 検索クエリ最適化アルゴリズム**

楽天商品検索APIから適切な家具を抽出するため、以下の手順で計測値から検索文字列を動的に生成・分割します。

* **基準範囲の定義**: ユーザーの計測値 M と許容誤差 T から検索下限値を算出し、対象区間 \[M-T,M\] を決定します。  
* **インクリメント処理**: 上記の区間内を0.5cm刻みでループ処理し、必要な寸法キーワード群（例: 幅41.5cm, 幅42.0cm）を配列として生成します。  
* **バッチ分割統合**: APIリクエストのURL文字数制限を回避するため、生成したキーワードを5件ごとに論理和（OR検索）で結合してグループ化します。  
* **非同期実行**: 構築した分割クエリリストを Coroutine を用いてバックグラウンドで順次送信し、アプリのフリーズ（処理落ち）を完全に防止します。

  ## **4\. UI排他制御とエラーハンドリング**

* **表示の排他処理**: 各種設定モーダルを展開した際は、背後にあるメインパネルの操作権限（Raycast Target）を無効化し、誤タップによる不具合を防ぎます。  
* **データの重複排除**: 複数回に分けたバッチ通信でJSONデータを受け取る際、抽出した itemUrl を一時的なハッシュテーブルに保存します。これにより、同一商品が再度ヒットした場合でもカードの重複生成をスキップできます。

## 

## 

## 

## 

## 

## 

## 

## **5\. 通信エラー・フェールセーフ仕様**

モバイル環境特有の通信切断やAPI側の障害発生時に、アプリがフリーズしたりユーザーを混乱させたりしないための例外処理仕様です。UnityWebRequest の実行ステータスを監視し、状況に応じた適切なフィードバックをUIに表示します。

### **5.1 エラー種別とシステム挙動**

| エラー種別 | 発生条件・検知方法 | UIフィードバックと内部処理 |
| :---- | :---- | :---- |
| **オフライン・通信切断** | リクエスト送信前または通信中にデバイスのネットワーク接続が失われた場合 (ConnectionError) | 「**ネットワーク接続を確認してください**」と表示。実行中の非同期通信（Coroutine）を即時停止し、後続のバッチ処理をキャンセル。 |
| **タイムアウト** | 通信環境の悪化により、一定時間（例: 10秒）経過してもAPIから応答がない場合 | 「**通信がタイムアウトしました。電波の良い場所で再度お試しください**」と表示。タイムアウトしたリクエストを破棄。 |
| **API・サーバーエラー** | 楽天APIサーバーのメンテナンス、アクセス集中、または不正なリクエストが発生した場合 (ProtocolError / HTTP 4xx, 5xx) | 「**検索サービスに一時的な問題が発生しています**」と表示。開発者向けにコンソールへHTTPステータスコードを内部記録（Log）。 |
| **JSONパースエラー** | APIからの返却データが予期せぬ形式や破損状態であった場合 (DataProcessingError) | 商品カードの生成処理を安全にスキップし、他の正常なデータのみを描画。すべて失敗した場合は「該当なし」として扱う。 |

### **5.2 処理中断時のUIリセットロジック**

エラー検知時は、ユーザーが何度でも再試行できるよう以下のリセット処理を強制実行します。

* **ローディング状態の解除**: （実装している場合）読み込み中のスピナーアイコンや「検索中…」の表示を即座に消去。  
* **フラグの初期化**: 検索実行中を判定するフラグ（isSearching 等）を強制的に false へ戻し、検索ボタンのロックを解除。  
* **残存UIのクリーンアップ**: エラー発生時点で途中まで生成されていた不完全な商品カードや、古い「該当なし」メッセージオブジェクトを全て破棄。

## **6\. Unity実装・シーン構成仕様**

本アプリケーションはUnityで構築されており、モバイルAR向けの各種パッケージとUIシステムを活用しています。

### **6.1 使用パッケージ・コンポーネント**

* **AR Foundation / ARCore XR Plugin (Android) / ARKit XR Plugin (iOS)**  
  * 空間認識、平面検出（AR Plane Manager）、および画面中央からのレイキャスト（AR Raycast Manager）による座標取得を担当します。  
* **TextMeshPro (TMP)**  
  * 解像度に依存しない高精細なテキスト描画を提供します。計測距離やUIの文字、商品名などに使用し、Auto Sizing 機能により枠内に文字が収まるよう自動調整しています。  
* **UnityWebRequest / Coroutine**  
  * 楽天APIとの通信処理を非同期（メインスレッドを止めない処理）で行い、通信中もARカメラの映像やUIのスクロールがカクつかない（フリーズしない）ように制御しています。

### **6.2 シーン階層構造（Hierarchy）**

プロジェクトのメインシーンにおける基本的なオブジェクト階層と役割です。

Plaintext

\[Scene: Main\_AR\]

 ├─ AR Session               ... AR機能のライフサイクル管理

 ├─ XR Origin (AR Rig)       ... ユーザーの基準座標

 │   └─ AR Camera            ... 端末のカメラ映像描画・レイキャスト発射元

 │

 ├─ AppManager               ... 制御スクリプトのアタッチ先

 │   ├─ kyori.cs             ... AR計測ロジック

 │   └─ RakutenSearch.cs     ... API検索・UI制御ロジック

 │

 └─ Canvas (UI)              ... 画面レイヤー (Screen Space \- Overlay)

     ├─ MainUI\_Panel         ... 計測ガイドテキスト、各種操作ボタン群

     ├─ TargetCrosshair      ... 画面中央の「＋」マーク（画面固定）

     ├─ Settings\_Panel       ... 設定ダイアログ（非アクティブ状態）

     ├─ Keyword\_Panel        ... キーワード選択ダイアログ（非アクティブ状態）

     └─ Result\_Panel         ... 商品結果パネル

         └─ Scroll View

             └─ Viewport

                 └─ Content  ... 【商品カードプレハブの生成先】

### **6.3 動的UIレイアウト制御 (uGUI)**

検索結果パネル（Scroll View）に表示されるアイテムカードは、取得した件数に応じて動的に生成・配置されるため、以下のUnity UIコンポーネントを組み合わせて自動整列させています。

| 対象オブジェクト | 使用コンポーネント | 設定目的・役割 |
| :---- | :---- | :---- |
| **Canvas** | Canvas Scaler | 端末の画面サイズ（アスペクト比）が異なっても、UIのサイズや配置が崩れないように自動スケール（Scale With Screen Size）させる。 |
| **Content**  (Scroll View内) | Vertical Layout Group | APIから取得して生成（Instantiate）された商品カード（ITEMCard）を、上から下へ等間隔で自動的に並べる。 |
| **Content** | Content Size Fitter | 生成された商品カードの数に合わせて、Content自体の縦幅（Height）を自動拡張し、正しくスクロールできるようにする。 |
| **ITEMCard**  (Prefab) | Layout Element | Vertical Layout Groupの配下に入った際に、カード自身の最小サイズや優先サイズを保つように制御する。 |

### **6.4 動的オブジェクトのメモリ管理**

商品検索を連続して実行した際、不要になったUIオブジェクトがメモリに蓄積するのを防ぐため、検索処理（SearchCoroutine）の冒頭で以下のクリーンアップ処理を実行しています。

1. **子要素の破棄**: Content オブジェクト配下に存在する古い商品カード（ITEMCard）を foreach ループで検索し、Destroy(child.gameObject) で全削除します。  
2. **メッセージの破棄**: 以前の検索で生成された「該当なし（NotFoundMessage）」オブジェクトを探し出し、再検索開始前に確実に破棄（メモリ解放）します。

